syntax = "proto3";

package tk.common;

message Timestamp {
  int64 seconds = 1; // Seconds since the Unix epoch.
  int32 nanos = 2; // Nanoseconds since the Unix epoch.
}

enum Role {
  ADMIN = 0;
  KIOSK = 1;
  STUDENT = 2;
  MENTOR = 3;
}
